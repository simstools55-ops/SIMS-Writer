# KN-OPS-010 Markdown Before/After

Use Markdown blockquotes for prose. Raw HTML may render as literal text, and code blocks may force horizontal scrolling. Preserve semantic paragraphs without display-only line breaks.
